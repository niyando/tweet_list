<!DOCTYPE html>
<html>
<head>
  <title>TweetList</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <%= content_for?(:head) ? yield(:head) : '' %>
</head>
<body>
<nav class="navbar navbar-expand-md navbar-dark bg-primary">
  <a class="navbar-brand" href="/">TweetBaba</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav mr-auto">
      <a class="nav-item nav-link <%= 'active' if params[:sort] != 'new'%>" href="/compilations?sort=top">Top</span></a>
      <a class="nav-item nav-link <%= 'active' if params[:sort] == 'new'%>" href="/compilations?sort=new">New</span></a>
    </div>
    <div class="navbar-nav ml-auto">
      <a class="btn btn-outline-light bg-transparent" href="<%= link_to_compile %>">+ Create</a>
    </div>
  </div>
</nav>

<div class="container">
  <%= yield %>
</div>

</body>

<%= javascript_tag do %>
  window.compilation_id = "<%= @compilation ? @compilation.id : nil %>";
<% end %>

</html>
